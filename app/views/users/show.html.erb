<h1 class="article-title"><%= @user.username %>'s Account</h1>
<div class="row">
  <div class="col-sm-4 col-sm-offset-4 text-center">
    <%= gravatar_for(@user, {size: 150}) %>
  </div>
</div>

<%= render 'articles/article_list', articles: @user.articles %>